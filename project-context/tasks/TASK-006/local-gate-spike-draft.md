# TASK-006｜本地相关性 Gate Spike（独立 DRAFT v1.1）

```yaml
required_reading:
  - AGENTS.md
  - project-context/context-manifest.md
  - project-context/CHIEF-BOOTSTRAP.md
  - project-context/current-state.md
  - project-context/product.md
  - project-context/decision-register.md
  - project-context/project-mainline-roadmap.md
  - project-context/handoff-and-task-state-machine.md
  - project-context/agent-response-protocol.md
  - project-context/role-wakeup-and-handoff.md
  - project-context/tasks/TASK-006/draft.md（v1.1，APPROVED）
  - project-context/tasks/TASK-006/route-b-decision.md（D-T006-ROUTE-B，APPROVED）
  - project-context/tasks/TASK-005A/release-qa-report.md（Run #28 证据）
  - eval/eval-contracts.md
  - eval/eval-policy-v1.md
  - v2/migrations/002_eval.sql（E001—E008 种子定义）
  - v2/app/src/lib/memory-config.ts
  - v2/app/src/lib/eval-program-rules.ts（E001/E004 程序判定）
  - v2/app/src/app/api/chat/route.ts（召回与写入产品路径）
doc_type: 任务 DRAFT（独立 Spike 草案；**未批准**——本文件不构成任何实施授权）
task_id: TASK-006（内部 Spike；不改变 TASK-006 状态）
spike_id: TASK-006-SPIKE-LOCAL-GATE
status: DRAFT（等待 Founder Review 1 再裁）
draft_version: v1.1（2026-08-12；v1.0 + Founder Review 1 CHANGES_REQUESTED 八项修订：独立信号证明 / 候选 2 可执行定义 / 连续分数统一验收 / 冻结 holdout / 合成数据与网络边界 / 预装依赖检查 / 延迟资源测量口径 / 审批卡只问批准）
drafter: operational-chief-2026-08-12-01（执行 Chief）
draft_date: 2026-08-12
basis: D-T006-1（DRAFT v1.1 APPROVED）+ D-T006-ROUTE-B（路线 B APPROVED）+ D-T006-LOCAL-SPIKE（规划基础已获 Founder 同意）+ Founder Review 1 CHANGES_REQUESTED（2026-08-12）
state_constraint: TASK-006 保持 APPROVED；TASK-004 保持 PAUSED；不启动 TASK-007 / TASK-005B；不修改产品代码、正式 8 Case、Schema、评测规则
```

---

## 0. Founder Review 1 修订记录（2026-08-12，CHANGES_REQUESTED → v1.1 修订）

> 本表逐项记录 Founder Review 1 八项意见及本 DRAFT v1.1 的对应修改位置。完整意见与修订均落盘本文件，不在聊天压缩。

| # | Founder 意见（摘要） | v1.1 修改位置 |
|---|---|---|
| 1 | 候选 1 与 mem0 同源 embedding 可能只是重复现有相似度；须证明独立信号，否则降为对照基线并重新定义真正候选；对照基线不计入"最多两个候选" | §3 候选机制 1 改为**本地词法/统计二次相关性 Gate（独立信号）**，信号源与 mem0 不共源（§3 候选 1"信号源"）；新增**独立性门**（Spearman ρ ≥ 0.9 → 降级基线对照，不作为正式候选）；§3 末尾与 §9 停止条件 9 明确**对照基线不计入"最多两个候选"**、降级后正式候选不足时停止并提交 CR 重新定义候选（由 Founder 裁决），不得自行凑数 |
| 2 | 候选 2 不能只写抽象"规则/类别"，须明确输入、主题体系来源、判定步骤、输出格式、0–1 分数，以及为何不退化为固定字面量规则 | §3 候选机制 2 重写为**可执行定义**：输入（q + m，mem0 score 仅只读背景）；主题体系来源（冻结 `spike/theme-system.md`，类别清单 + 判定关键词/模式 + 相容性矩阵）；判定步骤 ①②③④；输出 `score = w1·comp + w2·lex`（0–1 连续）；新增"为何不退化为字面量规则"论证（§3 候选 2"泛化要求"：类别基于通用主题词汇、禁止写死"天气→猫"式具体配对、holdout 未见话题验证、固定白/黑名单触发停止条件 3） |
| 3 | 每个正式候选必须产生可校准、可比较的连续分数；仅布尔输出的机制须单独定义验收，不得套用"分离边际 > 0.1" | §3 新增**统一要求**：每个正式候选输出 0–1 连续可校准分数；两个候选均满足，因此"分离边际 > 0.1"统一适用（§8 验收 3/4/5）；本 Spike 不引入仅布尔输出的机制 |
| 4 | 已公开 holdout 类型，不得称真正盲测；统一改称"冻结 holdout"：调参前锁定数据/标签/版本/哈希，最终只运行一次，见结果后禁调参，独立 Reviewer 须核对冻结时间和提交历史 | §5.3 全部改称**冻结 holdout**，新增**冻结纪律**（§5.3.4）：调参前锁定场景/标签/参数版本并记录内容哈希（`spike/holdout-freeze.md`）；最终只运行一次；运行后禁止继续调参；§8 验收 5 与 §9 停止条件 5 同步；**§5.3.7 新增独立 Reviewer 核对项**：Reviewer 必须核对冻结记录的时间戳与 Git 提交历史，确认冻结发生在调参前 |
| 5 | 只允许合成评测数据和评测专用 user_id；明确允许本机 loopback 服务，禁止所有非本机网络；不得读取、复制或落盘真实用户内容 | §5.1 数据边界重写：只允许合成评测数据 + 评测专用 user_id（`eval-<runShort>-<case>-<rand>`）；仅允许 loopback（127.0.0.1/::1）；**禁止一切非本机网络**；新增"**不得读取、复制或落盘真实用户内容**"（§5.1、§7 禁止 11、§7 允许 1/3） |
| 6 | 禁止运行时下载模型或权重；开始前必须核验本地模型、权重、版本和运行依赖已存在；缺失时立即停止并提交依赖/范围裁决，不得自行安装 | 新增 **§5.4 预装检查**（原被引用但缺失，现已补齐）：Spike 开始前核验清单（本地 mem0-server、PostgreSQL、Node 现有依赖、fastembed 模型缓存）与记录方式；缺失 → **立即停止**，按 §9 停止条件 6 提交范围/依赖裁决，**不得自行安装**；§7 禁止 10 同步 |
| 7 | 补齐性能测量口径：预热次数、正式样本次数、P95 算法、测量进程边界、基线资源、增量资源、峰值 RSS、CPU 时间；不得只写阈值不写如何测量 | §6.1 测量口径逐项补齐：预热 5 次、有效样本 ≥30、P95 = `ceil(0.95×N)` 位、进程边界定义、基线值（mem0 检索自身）、增量值（候选 − 基线）、峰值 RSS、CPU 时间；§6.3 报告要求含 P50/P95/P99 与基线/候选/增量 |
| 8 | Review 1 重新审批时只能询问一个问题："是否批准 DRAFT v1.1"；候选范围随该问题整体审批；执行模式必须在 DRAFT 批准后单独确认 | §11 待裁决事项缩减为**唯一问题**"是否批准 DRAFT v1.1"（候选范围并入整体审批）；§10 明确执行模式**不在本审批卡询问**，批准后单独确认；§12 下一交接同步 |

---

## 1. 任务目标（一句话）

在**不接入产品路径、不外发任何用户数据、仅使用已预装本地组件**的前提下，用独立、限时的技术 Spike 验证 **最多两个本地候选机制**能否可靠分离 E004 场景的无关召回与 E001 等正向召回，为 TASK-006 后续 Change Request 与实施计划提供可复现的技术可行性证据。

> Spike 通过只代表"技术可行"，**不代表 TASK-006 完成**，不改变 TASK-006 的 APPROVED 状态；产品实现必须另经 Founder 批准的新 Change Request 与实施计划。

## 2. 背景与已核验事实（证据锚点）

| 事实 | 证据 |
|---|---|
| E004 缺陷仍存在：Run #28 天气话题召回无关 2 条（失眠 0.431、橘猫 0.360，允许 ≤1），AI 回复被污染 | `release-qa-report.md` §5/§6；eval_runs.run_number=28 |
| 简单阈值无法可靠分离正负候选（校准集与 holdout 分离边际均未达 TASK-006 DRAFT v1.1 可靠性门槛） | `route-b-decision.md` §2.1（D-T006-ROUTE-B 裁决依据） |
| 外部模型 Gate 不作为产品路线：隐私边界、严格墙钟延迟、失败回退、证据治理不满足产品化要求；仅保留为离线研究证据 | `route-b-decision.md` §3.1/§3.2；外部 Gate v6 最大墙钟 4063ms，超时 keep-all 回退会恢复 E004 缺陷 |
| 产品召回链路：mem0.search top5 → score ≥ 0.35 过滤 → 注入 Dify；`used_memory` 只含过滤后结果，不能支撑离线扫描 | `chat/route.ts:55-58`；`memory-config.ts`（RECALL_THRESHOLD=0.35 / RECALL_TOP_K=5 / WRITE_MODE=async，产品与快照共用） |
| mem0 检索分数来源：本地 fastembed（`BAAI/bge-small-zh-v1.5`）向量相似度；**与候选机制的信号独立性必须实证，不得假设** | `TASK-005A/draft.md` §2.1（历史核验记录）；本 DRAFT §5.2 |
| E004 程序判定：`max_irrelevant_recall ≤ 1`，关键词 猫/吉他/失眠/小橘 | `eval-program-rules.ts:425-446`；`002_eval.sql:104-112` |
| E001 正向召回：`recall_min_related ≥ 1`（失眠 0.660 + 橘猫 0.445，Run #28 PASS） | `eval-program-rules.ts:401-423`；`release-qa-report.md` §5 |
| 强约束不得退化：E006 deletion、E007 safety、E008 privacy | `eval-policy-v1.md` §1；Run #28 全 PASS |
| 历史症状同源：2026-07-25 基线"天气不错→召回猫 37% + 吉他 36%" | `baseline-results.md` BC004（本问题长期存在） |

## 3. 候选机制（最多两个，本 Spike 内定义）

> 遵循路线 B：**不向外部模型或外部数据服务发送用户查询、Memory 内容或其他用户数据**。两个候选均为本地运行、仅访问 loopback 本地服务、不进入产品路径。Spike 期间不得扩展为第三个候选。
>
> **统一要求（Founder Review 1 第 3 条）**：每个正式候选必须输出**可校准的 0–1 连续相关性分数**（非二值判定），因此统一的"分离边际 > 0.1"验收（§8）对两个候选同样适用。

### 候选机制 1：本地词法/统计二次相关性 Gate（独立信号）

- **做法**：在本地 mem0 检索返回的候选（含 score）上，用**与 mem0 向量相似度不同源的本地词法/统计特征**计算"查询 ↔ 候选记忆"的二次相关度（0–1 连续分数），验证能否把 E004 无关候选与 E001 相关候选可靠分开；
- **信号源（与 mem0 不共源）**：基于文本表面的统计特征，如字符/分词 n-gram 重叠率、共现词比、BM25 类统计评分等；**信号实现不得调用 mem0、不得复用其向量相似度、不得依赖 bge 嵌入分数**；
- **为什么候选**：Founder Review 1 第 1 条——若二次机制与 mem0 使用同一 embedding 来源，只会重复现有相似度，不算候选机制。本候选必须提供**独立信号**，并通过 §5.2 独立性验证；
- **独立性门（实证）**：校准集上计算候选 1 分数与 mem0 原始 score 的 **Spearman 秩相关 ρ**。ρ ≥ 0.9 → 信号共源、无新增信息，**该机制降级为基线对照，不作为正式候选**（可继续报告但其结果不参与验收，且**不计入"最多两个候选"**）；ρ < 0.9 → 视为独立信号，作为正式候选参与验收。Spike 报告必须同时报告 ρ 与"独立信号带来的分离增益"（候选分离边际 vs mem0 原始分数分离边际）。
- **降级后处理（Founder Review 1 第 1 条）**：若候选 1 因独立性门失败而降级为基线对照，正式候选将不足两个（仅剩候选 2）。此时**停止 Spike 并提交 Change Request**，由 Founder 裁决是否重新定义真正的本地候选机制（替换或补充）后继续，**不得自行凑数新增候选，也不得把降级基线当正式候选通过验收**（见 §9 停止条件 9）。

### 候选机制 2：本地主题类别 Gate（可执行定义）

- **输入**：查询文本 `q` + 候选记忆文本 `m`（mem0 原始 score 仅作只读背景，**不参与本机制判定**）；
- **主题体系来源（冻结）**：Spike 启动时基于 8 Case 种子文本与冻结 holdout 场景（§5.3）人工归纳的主题类别清单，例如 `{宠物, 健康/睡眠, 兴趣/技能, 情感关系, 天气/日常, 财务, 身份信息, 其他}`；类别清单、每个类别的判定关键词/模式、类别相容性矩阵（如下）写入 Spike 文件 `spike/theme-system.md` 并**冻结（记录内容哈希）**；该体系只服务于本 Spike，不进入产品、不写入正式评测集；
- **判定步骤**：① 用冻结的关键词/模式规则对 `q` 做主题分类（可多主题，取置信最高）；② 对 `m` 做主题分类；③ 查询冻结的类别相容性矩阵得到相容度 `comp ∈ {0, 0.5, 1}`（1=同类或强相关，0.5=中性/弱相关，0=不相容）；④ 计算词法重叠 `lex ∈ [0,1]`（与候选 1 同源的文本统计特征，独立于 mem0）；
- **输出（0–1 连续分数）**：`score = w1·comp + w2·lex`，权重 `w1=0.7, w2=0.3`（Spike 启动时冻结，调参仅允许在校准集上调整 w1/w2 与相容性矩阵条目，见 §5.3 调参纪律）；
- **为何不会退化为固定字面量规则（Founder Review 1 第 2 条）**：① 判定对象是**主题类别**（宠物、健康/睡眠等）而非"天气→猫"之类的具体配对；类别判定词基于通用主题词汇与模式，禁止把"天气→猫/失眠/吉他"具体字面量配对写进 `theme-system.md`；② 每个类别须提供**多词模式**且 holdout 中未见过的话题（多义词"苹果"、他人属性"朋友分手"、隐式关联）会验证类别判定是否泛化；③ 若 Spike 过程中发现必须加入"天气/猫/失眠/小橘"等目标字面量才能通过，即视为针对固定字面量硬编码，触发停止条件 3；④ 规则内容与哈希在调参前冻结，Reviewer 核对冻结记录，防止事后偷偷加入目标字面量；
- **泛化要求**：规则必须对冻结 holdout 中的未见过话题（多义词、他人属性、隐式关联等）有效；固定关键词白/黑名单不视为解决方案（触发停止条件 3）。

> 候选机制的选择、组合与评估顺序由 Spike Builder 在实施计划中给出，但**不得超出上述两个候选**；若 Spike 中证明需要第三个机制、外部模型或**未预装组件**，停止并提交范围/依赖裁决（§9 停止条件 4/6）。

## 4. 限时（Timebox）

- **Spike 执行窗口**：自 Spike Builder 启动起 **≤ 5 个工作日**（墙钟），或 **≤ 3 轮"测量—调整—复测"迭代**（以先到者为准）；
- 时间盒到点即停止，不得无限调参；未收敛则按停止条件 8 处理；
- 基线测量 Run 与最终验收 Run 不计入 3 轮调整迭代（沿用 TASK-006 DRAFT v1.1 §6 口径）；
- 超过时间盒或迭代上限 → 停止 Spike，保留全部证据，返回 Chief/Founder 裁决。

## 5. 校准、信号独立性与冻结 Holdout（泛化边界）

### 5.1 数据边界（Founder Review 1 第 5 条）

- **只允许合成评测数据**：所有查询与种子记忆文本均由 Spike 内构造的合成内容构成（评测专用 user_id，格式沿用 `eval-<runShort>-<case>-<rand>` 隔离约定）；**禁止**导入真实聊天记录、真实用户 Memory 或生产库数据；**不得读取、复制或落盘任何真实用户内容**（含本地 traces、conversations、users 表真实行、日志中的真实输入/回复）；
- **网络边界**：仅允许访问 **loopback 本地服务**（127.0.0.1 / ::1，如本地 mem0-server、本地 PostgreSQL、本地 Next.js）；**禁止一切非本机网络**（无外网 IP、无公网 API、无远端模型/数据服务）；
- **禁止运行时下载**：Spike 全程**不得下载模型、权重或运行依赖**（无 pip/npm install、无 HuggingFace 拉取、无模型下载）；见 §5.4 预装检查；
- 网络边界验证写入 Spike 脚本与报告：连接目标白名单仅 loopback，运行期网络审计记录为证据（§8 验收 7）。

### 5.2 信号独立性验证（候选机制 1 专用）

- 校准集上计算候选 1 分数与 mem0 原始 score 的 **Spearman 秩相关 ρ**（定义与阈值见 §3 候选 1"独立性门"）；
- ρ ≥ 0.9 → 降级为基线对照，不作为正式候选；报告必须如实记录；
- 本验证只适用于候选 1（候选 2 与 mem0 信号源不同，不依赖此验证）。

### 5.3 校准集与冻结 Holdout（Founder Review 1 第 4 条——改称"冻结 holdout"）

1. **校准集**：E004 对抗场景 + E001/E002/E003/E005 正向场景的真实本地检索样本（每条候选含 score 与候选机制分数），三轮重复采集（mem0 检索非确定性，**禁止单轮结论**）；
2. **标签规则**：正样本 = 与查询相关的候选；负样本 = 与查询无关的候选；关键词初判 + **人工复核**（复核记录写入测量文件）；
3. **冻结 holdout 场景（3–5 个，不参与调参）**：真实本地检索、评测专用 user_id 建立合成种子记忆并检索、测量后清理；**不写入正式 eval_cases 种子、不修改正式 8 Case**；覆盖困难负例类型：多义词（"苹果"水果 vs 公司）、他人属性（"朋友分手"）、语义隐式关联（"失眠"与"橘猫半夜跑酷"）等；
4. **冻结纪律（取代"盲测"表述）**：holdout 场景、标签与候选机制参数在校准阶段开始前**锁定版本并记录内容哈希**（冻结记录写入 `spike/holdout-freeze.md`）；调参只允许在校准集上进行；**冻结 holdout 最终只运行一次**，运行后**禁止继续调参**；holdout 结果不达标时不得回头调参重跑，直接如实报告并触发停止条件 5/6（如适用）；
5. **可靠性判据（沿用 TASK-006 DRAFT v1.1 §3.1 定义，两个候选统一适用）**：校准集与冻结 holdout 各自 **F1 ≥ 0.9 且分离边际 > 0.1**（F1 = 2·P·R/(P+R)；分离边际 = 最低正例分 − 最高无关分；若实测波动 > 0.1，判据自动失效并触发停止条件 1）；
6. **样本量诚实声明**：8 Case + holdout 样本量小，Spike 报告必须如实声明样本量与置信局限，不夸大；holdout 不可建立或验证证据与校准数据不独立 → 触发停止条件 5。
7. **独立 Reviewer 核对（Founder Review 1 第 4 条）**：冻结记录必须包含**冻结时间戳与 Git 提交号**；独立 Reviewer 核对冻结记录与提交历史，确认 holdout 场景/标签/版本/哈希的冻结发生在任何调参之前、最终只运行一次、运行后无继续调参的提交。核对结论写入 Spike 报告（缺失 → 冻结纪律不成立，按停止条件 5 处理）。

## 5.4 预装检查（Founder Review 1 第 6 条；Spike 开始前的强制门）

- **原则**：Spike 全程**禁止运行时下载模型、权重或运行依赖**；开始前必须核验所有本地组件已存在，缺失时**立即停止**并提交范围/依赖裁决，**不得自行安装**（pip/npm install、HuggingFace 拉取、模型下载均禁止，见 §7 禁止 10）；
- **核验清单（开始前逐项记录）**：
  1. 本地 mem0-server 运行中且版本已知（loopback 可达）；
  2. 本地 PostgreSQL（ai_companion）运行中（loopback 可达，评测专用 user_id 可用）；
  3. Node/npm 现有依赖满足 Spike 脚本运行（不新增依赖）；
  4. 本地 embedding 模型缓存存在（fastembed `BAAI/bge-small-zh-v1.5` 已缓存；仅作为 mem0 检索能力使用，不直接作为候选信号源）；
  5. 网络边界：脚本运行环境确认仅 loopback 可访问（见 §5.1）；
- **记录方式**：核验结果（命令输出、版本号、路径、时间戳）写入 `spike/preflight-check.md` 并随 Spike 分支入库；任何一项缺失 → 立即停止，按 §9 停止条件 6 提交范围/依赖裁决。

## 6. 延迟与资源门（严格墙钟，Spike 内实测；口径按 Founder Review 1 第 7 条）

### 6.1 测量口径（每个候选机制统一执行）

| 口径项 | 定义 |
|---|---|
| 预热 | 候选机制函数先执行 **5 次**预热，结果丢弃不计 |
| 样本次数 | 每场景有效测量 **≥ 30 次**（或明确声明 N 与理由；N 与三轮测量对齐） |
| P95 计算 | 有效样本升序排序，取 `ceil(0.95 × N)` 位 |
| 进程边界 | 只测量候选机制函数调用本身的墙钟/CPU（不含脚本启动、DB 连接、模型加载）；资源测量以独立子进程峰值 RSS 或运行前后差值记录 |
| 基线值 | 同环境、同样本下 **mem0 检索自身的延迟/资源**（基线） |
| 增量值 | 候选机制附加开销 = 候选机制测量值 − 基线值（同一批样本） |

### 6.2 通过门

| 门 | 要求 | 测量方式 |
|---|---|---|
| 新增延迟上限（增量 P95） | **≤ 200ms**（候选机制自身开销） | 按 §6.1 口径实测；**不得把网络 timeout 当总截止时间** |
| 总预算上限（P95） | **≤ 1000ms**（基线 + 候选机制 + 探针开销） | 同上 |
| 资源门（内存） | 峰值 **≤ 512MB** | 子进程峰值 RSS 或运行前后差值 |
| 资源门（CPU） | 单次评估 **≤ 500ms CPU**（或等效说明） | 计时 + CPU 时间记录 |
| 失败回退 | 记录候选机制失败率与回退行为；**keep-all 回退会恢复 E004 缺陷，不得表述为修复** | 每轮记录失败/回退次数 |
| 无外发验证 | 脚本运行期间**零非本机网络调用**（仅 loopback） | 脚本内连接白名单 + 运行期网络审计记录 |

> 延迟与资源门是本 Spike 的**通过门**，不是产品最终承诺；产品实现的延迟目标由后续 CR/实施计划另行裁决（路线 B 约束：不默认接受明显增加的用户等待时间，见 CHIEF-BOOTSTRAP §7 升级边界第 4 条）。

### 6.3 报告要求

每轮迭代报告必须包含：预热次数、样本次数 N、P50/P95/P99（基线、候选、增量）、内存峰值（基线、候选、增量）、失败/回退次数、测量环境（机器、本地服务版本、进程边界声明）。

## 7. 允许 / 禁止范围

### ✅ 允许

1. 在 Spike 分支内新增**独立只读测量/验证脚本**：评测专用 user_id、真实本地 mem0 检索（loopback）、本地词法/统计特征计算；不调用 mem0.add（冻结 holdout 场景的种子记忆除外，测量后清理）；不落库到正式表、不进入 /api/chat 产品路径；
2. 读取正式代码与契约作为只读输入（memory-config.ts、chat/route.ts、eval-program-rules.ts、002_eval.sql、eval-contracts.md 等）；
3. 证据落盘：脚本 + 原始数据 + 报告 + 冻结记录（主题体系、holdout 冻结、哈希）写入 `project-context/tasks/TASK-006/spike/`（随 Spike 分支入库，可复现）；
4. 使用**已预装**的本地组件：本地 mem0-server（loopback）、本地 PostgreSQL（loopback）、本地 Node/npm 现有依赖、已缓存的 fastembed 模型文件（仅作为 mem0 检索能力，不直接作为候选信号源）；
5. 在获批的独立 Spike 分支上提交 Spike 过程与报告。

### ❌ 禁止

1. **修改产品代码**：`v2/` 下任何文件（含 memory-config.ts、chat/route.ts、eval-program-rules.ts）一律不动；
2. **修改正式 8 Case / Schema / 评测规则**：eval_cases 种子（002_eval.sql）、eval-policy、pass_criteria、判定逻辑、迁移全部不动；
3. **外发任何用户数据或访问非本机网络**：不向外部模型、外部数据服务或任何非 loopback 地址发送用户查询、Memory 内容或其他用户数据；不调用外部模型 API；
4. **不接入产品路径**：不把 Spike 机制写入 /api/chat 或任何产品行为；
5. **不触碰 TASK-007、TASK-005B、TASK-004**（TASK-004 保持 PAUSED，不重启删除拦截方案）；
6. **不修改治理文件**：current-state、decision-register、roadmap、AGENTS.md 等不在本 Spike 内改动；
7. **不接触生产密钥、不部署、不合并、不 force push**；
8. **禁止针对"天气/猫/失眠/小橘"等固定字面量硬编码过关**（固定关键词白/黑名单不视为解决方案）；
9. **不得以 Mock 替代最终真实验收**（验收必须是真实本地检索 + 真实特征计算的全链路执行）；
10. **禁止运行时下载模型、权重或依赖；禁止安装未预装组件**（见 §5.4）；
11. **禁止导入真实用户数据**（只允许合成评测数据）；
12. 历史 `feature/task-004-spike` 工作区（E:\正式作品 主检出）只记 W2（本地未同步），**禁止写入、同步或清理**。

## 8. 验收标准（Spike 通过门，全部满足才可提交"Spike 通过"结论）

1. **预装检查通过**（§5.4）：所有依赖已预装，无运行时下载、无安装动作；若有未预装项 → 不进入验收，按停止条件 6 提交范围/依赖裁决；
2. 候选 1 通过**信号独立性验证**（ρ < 0.9，§5.2），否则降级为基线、不参与验收；候选 2 通过可执行定义核对（输入/主题体系来源/判定步骤/0–1 分数齐全且冻结）；
3. 每个正式候选均输出 **0–1 连续可校准分数**，完成 **≥3 轮**真实本地检索测量，分数分布、分离边际与波动范围如实记录；
4. **校准集 F1 ≥ 0.9 且分离边际 > 0.1**（两个正式候选各自满足；定义见 §5.3.5）；
5. **冻结 holdout 一次性运行**：F1 ≥ 0.9 且分离边际 > 0.1；holdout 在调参前冻结（版本 + 哈希锁定），最终只运行一次，运行后无继续调参；样本量与置信局限如实声明；
6. **延迟门通过**：增量 P95 ≤ 200ms、总预算 P95 ≤ 1000ms（严格墙钟实测，口径见 §6）；
7. **资源门通过**：内存峰值 ≤ 512MB、CPU 单次 ≤ 500ms（或等效说明）；
8. **网络与数据边界验证通过**：仅 loopback 访问、零非本机网络调用、仅合成数据（审计记录）；
9. **零产品改动**：Spike 分支 diff 不包含任何产品代码、正式 8 Case、Schema、评测规则或治理文件修改；
10. **证据完整落盘**：脚本、原始数据（含 score 与候选机制分数）、人工复核记录、冻结记录（主题体系/holdout/哈希）、延迟/资源/网络审计证据、失败尝试记录均写入 `project-context/tasks/TASK-006/spike/`；
11. **报告诚实**：Spike 报告包含样本量声明、波动范围、失败尝试、候选对比、信号独立性结果（ρ）、与 TASK-006 DRAFT v1.1 §3.1 可靠性判据的对标，以及"Spike 通过 ≠ TASK-006 完成"的明确声明。

## 9. 停止条件（满足任一 → 停止 Spike，提交 Change Request / 返回 Founder，不得继续调参）

1. 正负候选**无法安全分离**：多轮测量后校准集或冻结 holdout 分离边际 ≤ 0.1（或实测波动 > 0.1 使判据失效），且无候选机制可拉开；
2. 修复 E004 **必然导致 E001 等正向召回明显退化**；
3. **必须削弱测试、修改判定规则或针对固定字面量硬编码才能变绿**；
4. 需要**超出批准范围**的数据库、架构、第三方系统、外部模型或**非本机网络**改动（含证明必须外发用户数据才能解决）；
5. **无法建立与校准数据独立的冻结 holdout 验证证据**（holdout 不可建立、不独立，或 holdout 一次性运行后不达标且已禁止继续调参）；
6. **本地模型、权重或运行依赖未预装**，或需要运行时下载/安装（Founder Review 1 第 6 条）——立即停止，提交范围/依赖裁决，**不得自行安装**；
7. **发现任何用户数据外发路径或非本机网络访问**（Spike 机制实际发起外部调用）；
8. **限时到期**（5 个工作日或 3 轮迭代，先到者）仍未收敛；
9. **候选机制不足或凑数**（Founder Review 1 第 1 条）：候选 1 独立性门失败降级为基线对照后，正式候选不足两个；或试图把对照基线、仅布尔输出的机制或针对固定字面量的规则充当正式候选通过验收——立即停止，提交 Change Request 由 Founder 裁决候选范围（重新定义真正候选），**不得自行凑数**。

## 10. 执行模式判断（预判——DRAFT 批准后单独确认，不在本审批卡询问）

```text
## 执行模式判断（Spike）

任务：TASK-006 本地相关性 Gate Spike（独立、限时、无外发、不接产品）
任务复杂度：中等（技术 Spike 需多轮测量—调整；不涉及产品代码与 Schema）
是否需要用户中途决策：是（测量结果出来后需裁决候选机制取舍；任何外发/扩范围/依赖需裁决）
是否预计多轮实现—验证—调整：是（先测量分布 → 机制调整 → 复测，预计 3+ 轮）
是否涉及高风险数据、权限或第三方服务：否（无外发、不触密钥、不接产品；仅 loopback 本地组件）
推荐模式：HANDOFF REQUIRED —— 长期 Builder 会话
建议会话名称：TASK-006｜Builder｜本地相关性 Gate Spike
任务分支：feature/task-006-local-gate-spike（Spike 实施时从 origin/main 新建；本 DRAFT 分支 feature/task-006-local-gate-spike-draft 仅承载本 DRAFT）
判断依据：技术 Spike 需要根据实验结果反复调整；需要用户中途查看测量结果并作决定；
          失败尝试与调整理由对后续 TASK-006 决策重要；单次子 Agent 上下文不足
```

> 本窗口只提出建议；**执行模式必须在 DRAFT 批准后单独确认**（Founder Review 1 第 8 条），本审批卡不询问执行模式。

## 11. 待 Founder 裁决事项（Review 1 重新审批）

1. **是否批准 DRAFT v1.1**（候选两机制含独立信号门与可执行定义、限时 5 工作日/3 轮、冻结 holdout、延迟增量 P95 ≤ 200ms / 总预算 ≤ 1000ms、资源 ≤ 512MB、校准+holdout 双 F1 ≥ 0.9 且分离边际 > 0.1、合成数据与 loopback-only 网络、预装检查、证据落盘 `TASK-006/spike/`）。

> 执行模式确认、Spike 分支创建等事项在 DRAFT 批准后另行裁决。

## 12. 下一交接

- Founder 批准 DRAFT v1.1（Review 1）→ 执行模式门单独确认 → 唤醒长期 Builder 会话（先交实施计划，含测量脚本设计与候选评估顺序，再执行）；
- 本窗口在 Founder 裁决前停止，不自行进入实施计划或 Builder 阶段。
